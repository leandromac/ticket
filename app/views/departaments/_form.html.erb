<%= form_for @departament, :html => { :class => "form-horizontal departament" } do |f| %>

    <% if @departament.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@departament.errors.count, "error") %> prohibited this departament from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @departament.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%= error_span(@departament[:title]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            departaments_path, :class => 'btn btn-default' %>

<% end %>
