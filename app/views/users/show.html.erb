<%- model_class = User -%>
<div class="page-header text-center">
  <h1><%= @user.name %></h1>
  <h4 class="text-muted"><%= @user.departament %>Suporte Técnico</h4>
</div>


              <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css" rel="stylesheet">
              <div class="container jumbotron">
              	<div class="row">
              		<div class="col-md-12">
                  	 <div class="profile">
                          <div class="col-sm-12">
                              <div class="col-xs-12 col-sm-6">
                                  <p><strong><%= @user.email %></strong></p>
                              </div>
                              <div class="col-xs-12 col-sm-6">
                                <p class="pull-right"><strong>Telefone: </strong><%= @user.phone %></p>
                              </div>
                          </div>

                          <div class="col-xs-12 divider text-center">
                              <div class="col-xs-12 col-sm-4 emphasis alert alert-success">
                                  <h1><strong> 205 </strong></h1>
                                  <p><small>ABERTO</small></p>
                              </div>
                              <div class="col-xs-12 col-sm-4 emphasis alert alert-info">
                                  <h1><strong>200</strong></h1>
                                  <p><small>FINALIZADO</small></p>
                              </div>
                              <div class="col-xs-12 col-sm-4 emphasis alert alert-danger">
                                  <h1><strong>5</strong></h1>
                                  <p><small>CANCELADO</small></p>
                              </div>
                          </div>

                  	 </div>
              		</div>
              	</div>
              </div>

              <%= link_to t('.back', :default => t("helpers.links.back")),
                            users_path, :class => 'btn btn-default'  %>
              <%= link_to t('.edit', :default => t("helpers.links.edit")),
                            edit_user_path(@user), :class => 'btn btn-warning' %>
              <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                            user_path(@user),
                            :method => 'delete',
                            :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                            :class => 'btn btn-danger btn-xs pull-right' %>

<hr>
